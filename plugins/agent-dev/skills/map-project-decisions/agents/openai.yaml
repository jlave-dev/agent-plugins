interface:
  display_name: "Map Project Decisions"
  short_description: "Shape software projects before implementation"
  default_prompt: "Use $map-project-decisions to map the first usable slice, separate human gates from agent-owned decisions, and identify what to investigate or defer."
