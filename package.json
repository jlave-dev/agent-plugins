{
  "name": "agent-plugins",
  "version": "1.14.1",
  "description": "Installable agent plugins and a Codex plugin marketplace",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --test scripts/*.test.js",
    "release": "semantic-release",
    "release:dry-run": "semantic-release --dry-run --no-ci"
  }
}
